replicaCount: 2

image:
  repository: perish95/jenkins-project  # 내 DockerHub ID로 변경
  pullPolicy: IfNotPresent
  tag: "5"  # Jenkins에 의해 자동 수정되는 항목

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080