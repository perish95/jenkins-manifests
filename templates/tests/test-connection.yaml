apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-manifests.fullname" . }}-test-connection"
  labels:
    {{- include "jenkins-manifests.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkins-manifests.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
